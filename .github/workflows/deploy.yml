name: Deploy GitHub Pages

on:
  push:
    branches:
      - main # Trigger this workflow on push to the 'main' branch
  workflow_dispatch: # Allow manual triggering of this workflow

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Check out the repository
      - name: Check out the repository
        uses: actions/checkout@v4

      # Step 2: Set up Ruby if you're using Jekyll
      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: 3.2 # Set your Ruby version

      # Step 3: Install dependencies (Jekyll or Bundler)
      - name: Install dependencies
        run: |
          gem install bundler
          bundle install

      # Step 4: Build the site (adjust for your build command)
      - name: Build the site
        run: |
          JEKYLL_ENV=production bundle exec jekyll build

      # Step 5: Upload the build artifact to GitHub Pages
      - name: Upload Artifact for Pages
        uses: actions/upload-pages-artifact@v1
        with:
          path: ./_site # Your build output directory

  deploy:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write

    steps:
      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v4
